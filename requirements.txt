# Minimal Video Processing App Requirements
Flask==3.0.0

# OpenCV (headless version to avoid libGL issues in Codespaces)
opencv-python-headless==4.10.0.84

# NumPy compatible with TensorFlow and MediaPipe
numpy==1.26.4

moviepy==1.0.3

# TensorFlow compatible with NumPy 1.26
tensorflow==2.19.1

# RawNet audio processing dependencies
torch>=2.1.0
torchaudio>=2.1.0

# Audio backend dependencies for newer PyTorch versions
librosa>=0.10.0
soundfile>=0.12.0

# Face overlay processing
mediapipe==0.10.21

pandas>=2.1.0
